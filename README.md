# icp
Python code for aligning 3D LiDAR scans of a damaged brick with an intact reference using ICP (Iterative Closest Point) algorithms. It converts the scanned meshes into point clouds and compares the two point clouds to isolate missing geometry by aligning the healthy brick to the damaged brick. 
